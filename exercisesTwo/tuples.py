# Tuple: ordered, immutable, allows for duplicate elements
# Can be more efficent than lists in some situations.


mytuple = tuple("Max", )

my_tuple = ('ab','b','c')

print(my_tuple)
print(my_tuple[::])


item = mytuple[0]
print(item)